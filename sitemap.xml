<?xml version="1.0" encoding="UTF-8"?>
<!--
  URL이 이 한 줄뿐인 이유: 런치데스크는 #/ 해시로 화면을 구분하는 단일 페이지
  앱이고(app.js의 BUILT/TITLES 라우팅 테이블 참고), 해시(#...)는 브라우저가
  서버에 요청을 보내기 전에 제거하므로 검색엔진은 애초에 받아볼 수 없다
  (WHATWG URL 표준 동작 — 이 앱만의 특이사항이 아니다). 즉 "#/start",
  "#/resources" 같은 경로는 색인 가능한 별도 URL이 아니라 전부 같은 루트
  문서(https://launchdesk.co.kr/) 하나로 수렴한다. 저장소에 서버 라우팅/리라이트
  설정(vercel.json 등)도 없어 실제로 응답하는 정적 경로도 "/"뿐이다. 나중에
  각 화면이 실제 서버 라우트(History API + 서버 렌더링/리라이트)로 바뀌면,
  그때 그 URL들을 여기 추가해야 한다.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://launchdesk.co.kr/</loc>
    <lastmod>2026-09-23</lastmod>
  </url>
</urlset>
