# 런치데스크(LaunchDesk) 프로젝트 인수인계 문서

> 이 문서는 실제 배포된 아티팩트 소스코드(단일 HTML 파일, 약 3,150줄)를 직접 읽고 검증하여 작성했습니다.
> 코드로 확인되지 않는 항목은 전부 **"기획상 예정"** 또는 **"미확정"**으로 명시했습니다. 추측성 서술은 없습니다.

- **현재 배포 URL**: `https://claude.ai/code/artifact/f5fa0fe1-793b-409d-95c6-96b55a4c1a2b` (Claude Artifact, 공개 공유 활성화 상태)
- **소스 형태**: 단일 `.html` 파일 하나 (프레임워크/빌드 없음, HTML+CSS+JS 인라인)

---

## 1. 프로젝트의 최종 목표

1인 쇼핑몰 창업 로드맵(대표자 본인의 실제 경험 — 자본금 2,000만원으로 시작해 5개월 만에 월매출 1억, 이후 브랜드를 8,000만원에 매각한 실사례)을 콘텐츠화하고, 여기에 실사용 가능한 운영 도구(마진 계산기·수익 시뮬레이터·광고 기록)와 대행 서비스(세팅 대행)를 결합한 **유료 전환 가능한 창업 교육 + 도구 제품**으로 만드는 것.

- 최종 수익모델(구독/1회구매/프리미엄 등)은 **미확정** — 아래 15번 항목 참조.

## 2. 이 서비스를 왜 만드는지

대표자가 원저작자인 노션 강의("월 매출 1억 쇼핑몰 로드맵 템플릿")를 대표자 본인이 직접 재구성한 것으로, 일반적인 창업 정보 콘텐츠와 달리 **실제 본인이 겪은 숫자와 실패/성공 경험**을 근거로 한다는 점이 차별점. 여기에 "읽기만 하는 콘텐츠"에서 끝나지 않고 실제 계산이 되는 도구(마진 계산기, 시뮬레이터, 광고 기록)를 붙여 재방문·실사용 가치를 만들려는 의도가 코드 구조(진행률 추적, localStorage 저장 도구들)에서 확인됨.

## 3. 핵심 사용자

코드/콘텐츠상 확인되는 타겟: **사업자 등록 전~직후 단계의 1인 쇼핑몰 예비 창업자**, 특히 완제품 사입 기반 의류 쇼핑몰을 염두에 둔 사용자(동대문 사입, 카페24, 스마트스토어 등 실무 용어 위주). 부업/전업 여부, 연령대, 성별 등 구체적 페르소나 세그먼트는 **미확정**(문서화된 바 없음).

## 4. 현재 MVP 범위

- 로그인/회원가입 **없음** (버튼은 있으나 클릭 시 "준비 중" 안내 모달만 뜸)
- 결제/구독 **없음**
- 실제 매출·주문 데이터 연동 **없음**
- 전 챕터 및 전 기능이 **누구에게나 무료로 공개**된 프리뷰 상태
- 서버/DB **없음** — 순수 정적 파일 + 브라우저 `localStorage`

## 5. 지금까지 확정된 기능 (코드로 확인됨)

| 영역 | 내용 |
|---|---|
| 시작 가이드 | 00~08 총 9개 챕터, 전부 무료 공개 |
| 진행률 추적 | 챕터별 완료 처리 3가지 방식(체크박스/워크시트/체크리스트) → 통합 진행률 % |
| 자료실 | 외부 링크 18개(사입·마케팅·상품마진·CS 카테고리) + 실무 설정 가이드 아코디언(6단계) |
| 운영 도구 | 대시보드(예시 데이터) / 수익 시뮬레이터(실계산) / 광고 기록(실저장) 3탭 |
| 마진 계산기 | 챕터03(2배 법칙 간이버전) + 운영도구(수수료·배송비 포함 정밀버전), 결과 저장(최근 5개) |
| 세팅 대행 | 3단계 신청 플로우(플랜선택→정보입력→mailto 발송) — **실 수신 이메일 미설정** |
| 로그인 모달 | UI만 존재, 제출 시 "준비 중" 안내로 대체 |
| 홈 대시보드 | "이어서 하기" 배너(실제 다음 미완료 챕터로 이동), 서비스 바로가기 |

## 6. 나중에 추가할 기능 (기획상 예정 / 대화에서 언급됨)

- 실제 로그인·회원가입, 계정별 데이터 분리
- 결제/구독 시스템 (모델 자체가 미확정 상태)
- 실제 쇼핑몰 연동 매출·마진율·주문건수 데이터(현재는 전부 "예시 데이터" 라벨)
- 카페24 주문내역 API 연동 — 교환/반품 접수 시 자동 체크 + 송장번호 표시 (사용자가 실현 가능성만 질문한 단계, **개발 착수 전, 별도 백엔드 필요**)
- 콘텐츠 유료 게이팅 (현재는 UI 배지만 있고 실제 접근 제한 없음)
- 자료실 내 "출시 예정" 표시된 3개 자료: SNS 콘텐츠 캘린더, 상세페이지 기획안 프레임, CS 응대 스크립트

## 7. 전체 사용자 흐름

```
홈(진입) → 이어서 하기 배너로 다음 미완료 챕터 확인
  → /start (9개 챕터 목록, 뷰어뱃지·소셜프루프 노출)
    → 챕터 00~08 순차 진행 (체크/워크시트/체크리스트로 완료 처리 → 진행률 상승)
  → 자료실 (상시 참고, 카테고리 필터)
  → 운영 도구 (마진 계산기 / 수익 시뮬레이터 / 광고 기록 실사용)
  → (선택) 세팅 대행 신청 → mailto로 신청서 발송
  → (시도 시) 로그인 클릭 → "준비 중" 안내
```

## 8. 화면별 기능 (실제 라우트 기준, hash 라우팅)

| 경로 | 화면 | 핵심 기능 |
|---|---|---|
| `/` | 홈 | 이어서 하기 배너(5단계 마일스톤 링), 서비스 목록, 챕터 미리보기 4개 |
| `/start` | 시작 가이드 목차 | 9챕터 카드(클릭 확장형), 필터(전체/무료/유료 — 현재 전부 무료), 진행률 바 |
| `/start/intro` ~ `/start/wrapup` | 챕터 00~08 | 각 챕터 1개씩 고유 인터랙션(탭/진단도구/타임라인/여정그래프 등) + 완료 처리 UI |
| `/tools` | 운영 도구 | 대시보드(예시)·시뮬레이터(실계산)·광고기록(실저장) 3탭 |
| `/resources` | 자료실 | 카테고리 필터 + 리소스 카드 18개 + 설정 가이드 아코디언 |
| `/services/setup` | 세팅 대행 | 플랜 선택 → 정보 입력 → mailto 발송 3단계 |
| `/login` | (모달) | 실제 라우팅 아님, 클릭 시 로그인 모달 오픈 |
| 그 외 (`/contact`,`/terms`,`/privacy`,`/business-info`,`/guide`) | 준비 중 | 공통 "이 페이지는 준비 중" 안내로 대체 |

## 9. 프론트엔드 구조

- **프레임워크 없음** — 순수 HTML/CSS/Vanilla JS 단일 파일
- **라우팅**: `location.hash` 기반 SPA. `BUILT` 객체(경로→섹션ID 매핑)에 등록된 경로만 실제 렌더링, 나머지는 "준비 중" 화면으로 폴백
- **이벤트 처리**: `document`에 등록한 단일 delegated `click` 핸들러 체인(탭/아코디언/필터/모달 등 전부 이 안에서 분기 처리)
- **디자인 시스템**: CSS 커스텀 프로퍼티(`:root`, 30개 변수) 기반 — 배경/사이드바/서페이스/잉크(텍스트)/accent(브릭 레드)/badge 4색 팔레트
- **차트/시각화**: 외부 라이브러리 없이 인라인 SVG로 직접 구현(원형 진행률, 도넛차트, 영역차트, 게이지)
- **상태 저장**: 전부 브라우저 `localStorage` (아래 12번 참조), 서버 통신 없음(코드 전체에 `fetch`/`XMLHttpRequest` 0건)
- **이미지 자산**: Claude Artifact의 asset 스토리지(`/_blob/{id}`)에 업로드된 8장 (챕터01 촬영전략 4장 + 챕터04 AI생성 촬영각도 4장)

## 10. 백엔드 구조

**없음.** 서버, API 엔드포인트, 인증 시스템, 데이터베이스 전부 미구축. 세팅 대행 신청서도 서버 저장이 아니라 사용자 브라우저에서 `mailto:` 링크를 여는 방식(사용자가 직접 이메일 앱에서 [보내기]를 눌러야 완성)으로 처리됨. **기획상 예정**: 실 계정/결제/데이터 연동을 위해서는 별도 백엔드가 전제조건.

## 11. 사용 API 및 외부 서비스

- **런타임에 실제로 호출하는 외부 API: 0개**
- 자료실의 외부 링크(신상마켓, 신상스튜디오, 셀러오션, 메타 광고 관리자, GA4, 메타 광고 라이브러리, 알파리뷰, 킵그로우, 채널톡, 이지어드민, 구글시트 3개)는 전부 **단순 하이퍼링크**일 뿐 연동이 아님
- 개발 과정에서 챕터04 이미지 생성에 이미지 생성 도구를 1회성으로 사용했으나, 이는 제작 단계의 일회성 작업이지 서비스 런타임에 연동된 API가 아님
- **미확정**: 카페24 API, 결제 PG, 실제 이메일 발송 서버 등 전부 향후 과제

## 12. DB에 저장할 데이터 구조

**실 데이터베이스 없음.** 브라우저 `localStorage`에 사용자(브라우저)별로 다음 키들만 저장되며, 로그인이 없으므로 계정 간 연동/동기화 개념 자체가 없음(다른 브라우저·기기에서는 전부 초기화):

| localStorage 키 | 형태 | 내용 |
|---|---|---|
| `ld-completed-chapters` | `string[]` | 완료 처리된 챕터 경로 배열 (진행률 산출 기준) |
| `ld-worksheet-start-prepare` | `{0..5: string}` | 챕터01 워크시트 6개 입력값 |
| `ld-checklist-start-setup` | `number[]` | 챕터02 체크리스트(7항목) 체크 인덱스 |
| `ld-checklist-start-marketing-setup` | `number[]` | 챕터05 체크리스트(9항목) 체크 인덱스 |
| `ld-first-visit` | timestamp | 최초 방문 시각 (D+ 카운터 산출용) |
| `ld-tools-calc-history` | `{cost,price,fee,ship,profit,marginPct,date}[]` (최대 5개) | 운영도구 마진 계산기 저장 이력 |
| `ld-adlog-records` | `{id,date,name,spend,revenue,channel}[]` | 광고 기록 로그 |

**미확정**: 실 계정이 붙을 때 이 구조를 서버 DB 스키마로 어떻게 이관할지는 아직 설계되지 않음.

## 13. 현재 개발된 부분

5, 8번 항목의 모든 화면·기능. 요약하면: **콘텐츠(9챕터+자료실)는 100% 완성, 도구(마진계산기·시뮬레이터·광고기록)는 실계산·실저장까지 완성, 계정/결제/실데이터 연동은 0%.**

## 14. 아직 개발되지 않은 부분

- 로그인/회원가입/계정 시스템 (UI 껍데기만 존재)
- 결제/구독/콘텐츠 유료 게이팅 (배지만 있고 실제 잠금 없음)
- 실제 매출·마진율·주문건수 데이터 연동 (전부 "예시 데이터" 라벨)
- 카페24 등 쇼핑몰 플랫폼 API 연동
- 세팅 대행 신청서 실 수신(현재 `CONTACT_EMAIL` 변수가 빈 문자열)
- 자료실 "출시 예정" 3개 자료 실물
- `/contact`, `/terms`, `/privacy`, `/business-info`, `/guide` 5개 페이지 (전부 "준비 중" 폴백)

## 15. 앞으로 개발할 우선순위

**미확정 — 공식적으로 합의된 우선순위표는 없음.** 지금까지 대화에서 언급된 순서를 참고용으로만 정리하면:

1. 세팅 대행 실 수신 이메일 설정 (`CONTACT_EMAIL`) — 코드 한 줄만 채우면 되는 최소 작업
2. 수익모델(구독/1회구매/무료+업셀 등) 확정
3. 로그인/계정 시스템 구축
4. 결제 연동 + 콘텐츠 실제 게이팅
5. 카페24 등 실 데이터 연동(매출/주문/교환·반품)

## 16. 중요하게 지켜야 할 개발 규칙

대화 중 사용자가 명시적으로 지시했거나, 문제 발견 후 합의된 규칙:

1. **실제 데이터가 없는 지표(매출/마진율/주문건수 등)는 반드시 "예시 데이터입니다" 라벨을 붙인다.** 로그인된 것처럼 가정해도 값 자체를 진짜처럼 보이게 만들지 않는다.
2. **로그인 없는 상태에서 개인화된 답변이나 진행 상태를 `localStorage`에 "몰래" 자동 저장하지 않는다** — 같은 브라우저를 다른 사람이 열면 남의 데이터가 보이는 문제가 생기기 때문. 단, 사용자가 명시적으로 액션(체크박스 클릭, 계산 결과 저장 등)을 취한 경우는 저장 허용.
3. **AI로 생성한 이미지는 반드시 "AI 생성 예시입니다" 디스클레이머를 표기한다.**
4. **다크패턴(가짜 조회수·구매자수 등)은 사용자가 명시적으로 승인한 범위 안에서만, 그 이상 확장하지 않는다.** (실제 사례: 프로모 배너에 임의로 할인가·카운트다운을 추가했다가, 사용자 승인 없이 만든 것이라 판단해 제거하고 "무료 프리뷰" 안내로 되돌린 적 있음)
5. **한 컴포넌트를 다른 컴포넌트로 전면 교체할 때는 이전 클래스명이 파일 전체에 남아있지 않은지 확인한다** — 이번 정리에서 옛 컴포넌트(`.chapter-card`, `.pick-panel`, `.res-list`, `.service-row` 등)의 CSS/JS가 삭제되지 않고 방치되어 있던 것을 다수 발견·제거함.

## 17. 지금까지 요청하면서 변경된 주요 결정사항 (시간 순)

1. **디자인**: 다크(보라/네온) 테마 → 라이트 테마 + 좌측 사이드바 구조로 전면 전환, 파란색 accent 거부 → 브릭레드(#b23a2e) 계열로 확정 (이후 유지 지시)
2. **콘텐츠 구조**: 원본 노션 강의를 그대로 옮기지 않고 카드형으로 압축 재구성하기로 결정 (처음엔 저작권 문제로 판단했으나, 원저작자가 대표자 본인임이 확인되며 법적 필요성은 사라졌고 스타일만 유지)
3. **챕터 구성**: 8개 → 9개(00~08)로 재편, "마케팅 인프라 세팅"을 별도 챕터(05)로 신설
4. **진행률 로직**: 페이지 방문 기반 → 체크박스 기반 → **현재: 챕터별 명시적 완료 액션(체크박스/워크시트/체크리스트 3가지 혼합) 기반**으로 두 차례 변경
5. **가격 정책**: "월 19,900원 구독"으로 확정 언급했던 단계 → **현재: 전 챕터 무료 공개, 정식 오픈 후 유료 전환 가능, 당장은 세팅 대행으로 업셀**로 피벗 (구독가는 현재 코드 어디에도 없음)
6. **다크패턴 요소**: `/start` 페이지의 "지금 N명이 보고 있어요"/"N명이 시작했어요"는 예시 단계 한정으로 승인, **별점·리뷰 카운트는 명시적으로 제외**
7. **디자인 레퍼런스 전환**: 자체 제작 목업 → 사용자가 별도로 만든 Figma Make AI 프로토타입 스크린샷을 실제 레이아웃 기준으로 채택(홈 구조, 운영도구 3탭 등)

---

## 프로젝트 인수인계용 요약 (다른 AI가 이것만 읽고 바로 이어서 상담 가능하도록)

**런치데스크(LaunchDesk)**는 대표자 본인의 실제 창업 경험(자본금 2,000만원 → 5개월 만에 월매출 1억 → 8,000만원에 매각)을 바탕으로 만든 **1인 쇼핑몰 창업 로드맵 + 운영 도구** 제품이다. 현재 상태는 **단일 HTML 파일로 만든 정적 프론트엔드 프로토타입(Claude Artifact로 배포)**이며, 서버·DB·로그인·결제가 전혀 없고 모든 사용자 상태는 브라우저 `localStorage`에만 저장된다.

콘텐츠(9개 챕터 + 자료실 18개 링크)는 완성 단계이고, 운영 도구 중 마진 계산기·수익 시뮬레이터·광고 기록 3가지는 실제로 계산·저장까지 동작한다. 반면 대시보드의 매출/마진율/주문건수, `/start` 페이지의 조회수·시작자수 등은 전부 **눈속임 없이 "예시 데이터"로 라벨링된 가짜 숫자**다.

**지금 이 시점에서 다음 개발자(혹은 AI)가 알아야 할 가장 중요한 3가지**:
1. 가격 정책은 "전 챕터 무료 프리뷰"로 최종 피벗된 상태이며, 이전에 언급됐던 "월 19,900원" 같은 숫자는 더 이상 유효하지 않다 — 코드에 없다.
2. 실 데이터/계정/결제가 붙기 전까지는 **어떤 지표도 진짜처럼 보이게 만들면 안 된다**(16번 규칙). 이 원칙 때문에 이번 세션에서 실제로 배너 하나를 되돌린 전례가 있다.
3. 다음 개발의 실질적 병목은 **백엔드가 전무하다는 것** — 로그인, 결제, 카페24 연동 중 무엇을 먼저 하든 별도 서버/DB 설계가 선행되어야 하며, 이 문서 작성 시점까지 그 설계는 전혀 시작되지 않았다(전부 미확정).
